spring:
  profiles:
    default: local
    include: secret
  jpa:
    database:
      mysql
    generate-ddl:
      false
    hibernate:
      ddl-auto:
        none
    show-sql:
      true
    properties:
      hibernate:
        format_sql:
          true
